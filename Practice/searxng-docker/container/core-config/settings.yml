# Read the documentation before extending the defaults:
# https://docs.searxng.org/admin/settings/

use_default_settings: true

search:
  formats:
    - html
    - json

server:
  secret_key: "xGRnaMzhGEga36ijjZSrzV8UTnkcx252"
  image_proxy: true
